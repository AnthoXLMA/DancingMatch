<div class="navbar-yourjob" style="box-shadow: 10px 3px 25px #bababa;  justify-content:space-between;">
  <div class="logo">
    <div class="logo-text">
      <h1>Dancing Partners</h1>
    </div>
  </div>
<%= link_to "Back", :back %>
<%= link_to "Home", root_path %>
<%= link_to "My profile", profile_path %>
</div>

<div class="container">
  <%= simple_form_for :appointments, url: appointments_path do |f| %>
    <div class="container_one">
      <%= f.input :start_on, as: :date %>
      <%= f.input :end_on, as: :date %>
      <%= f.input :name, placeholder: "Donne un nom à ton rendez vous" %>
      <%= text_field_tag :address, params[:address], class: "form-control", placeholder: "Où vas tu danser?" %>
      <%= text_field_tag :dance_id, params[:dance_id], class: "form-control", placeholder: "Quelle danse vas tu danser?" %>
      <%= f.button :submit, method: :post %>
    </div>
  <% end %>
</div>
