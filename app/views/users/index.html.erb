<div class="container-card-profile" style="display:flex;">
  <div class="contain-2">
    <% @users.each do |i| %>
    <div class="card-community">
        <div class="image-user-card">
          <%= image_tag i.photo %>
        </div>
        <div class="user-details">
          <ul>
            <li><%= link_to i.first_name, user_profiles_path(i) %> <%= i.age %></li>
            <li><%= i.city %></li>
            <li><%= link_to "See More Details", user_profiles_path(i) %></li>
          </ul>
        </div>
    </div>
    <% end %>
  </div>
  <div class="colonne-droite">
    <div class="contain-3">
      <div class="card-search-types">
      <%= simple_form_for users_path, method: :get do |f| %>
        <%= text_field_tag :query, params[:query], class: "form-control", placeholder: "Où?" %>
        <%= submit_tag "Search", class: "btn btn-primary" %>
      <% end %>
      </div>
    </div>
    <div class="contain-3">
      <div class="card-search-types">
        <input type="checkbox" name="gender" value="lady" /> Lady
        <input type="checkbox" name="gender" value="gentleman" /> Gentleman
      </div>
    </div>
    <div class="contain-3">
      <div class="card-search-types">
        <%= simple_form_for users_path, method: :get do %>
        <%= text_field_tag :query, params[:query], class: "form-control", placeholder: "Qui? quoi?" %>
        <%= submit_tag "Search", class: "btn btn-primary" %>
      <% end %>
      </div>
    </div>
    <div class="contain-3">
      <div class="card-search-types">
      critères de recherche n°3: quand?
      <%= form_tag users_path, method: :get do %>
        <%= text_field_tag :query, params[:user], class: "form-control", placeholder: "recherche par pseudo" %>
        <%= submit_tag "Search", class: "btn btn-primary" %>
      <% end %>
      </div>
    </div>
    <div class="contain-4">
      <div class="card-search-types">
      </div>
    </div>
  </div>
</div>
